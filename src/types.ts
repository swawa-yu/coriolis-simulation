export type Position = {
    x: number;
    y: number;
    z: number;
};

export type PositionGeo = {
    lat: number;
    lon: number;
};

export type PositionThree = {
    x: number;
    y: number;
    z: number;
};